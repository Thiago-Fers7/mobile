import { DefaultLayout } from "@components/layouts/default-layout";
import { Typography } from "@components/typography";

export function CreateContact() {
  return (
    <DefaultLayout>
      <Typography variant="heading">Create Contact Screen</Typography>
    </DefaultLayout>
  );
}

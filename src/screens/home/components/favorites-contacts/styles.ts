import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 150,
  },
  contentContainer: {
    padding: 16,
    paddingTop: 0,
    gap: 12,
  },
  listItem: {
    padding: 12,
    width: 220,
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 8,

    display: "flex",
    justifyContent: "center",
  },
});
